import type { MasterTableConfig } from "@/types/table"

export const duplicatedSampleConfig: MasterTableConfig = {
  id: "geology-sample-duplicated-mral",
  endpoint: "/api/analytics/sample-duplicated-mral/",
  defaultQuery: {},
}
