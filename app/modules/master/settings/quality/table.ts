
import type { MasterTableConfig } from "@/types/table"

export const qualityConfig : MasterTableConfig = {
  id: "master-quality-config",
  endpoint: "/api/master/quality-config/",
  defaultQuery: {},
}