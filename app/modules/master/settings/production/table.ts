
import type { MasterTableConfig } from "@/types/table"

export const productionConfig : MasterTableConfig = {
  id: "master-production-config",
  endpoint: "/api/master/production-config/",
  defaultQuery: {},
}